# Task 1 — Customer ordering flow

Implement the complete public customer journey on top of the existing baseline: working Add to cart, React Context cart with localStorage, `/cart`, `/checkout`, POST `/api/orders`, server-side repricing from product IDs/quantities, simulated card validation, store only `last4`, create `pending` order, confirmation route, clear cart after success. No customer login, DB, ORM, payment SDK or heavy state library. Preserve integer cents. Follow DESIGN.md. Run lint and build.
